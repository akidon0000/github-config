name: Sync Labels
on:
  workflow_dispatch:
  schedule:
    - cron: "0 10 * * 1"
  push:
    branches: [main]
jobs:
  sync-labels:
    permissions:
      issues: write
    runs-on: ubuntu-latest
    steps:
        - name: Checkout the current repository
          uses: actions/checkout@v4

